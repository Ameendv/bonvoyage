



<div class="col-lg-10 ms-auto pt-5 mt-3" style="background-color: #FFE8D2;height: 100%;">
<table border="0" cellspacing="5" cellpadding="5">
        <tbody><tr>
            <td>From:</td>
            <td><input type="text" id="min" name="min" placeholder="Please select a date"></td>
       
            <td>To:</td>
            <td><input type="text" id="max" name="max" placeholder="Please select a date"></td>
            
        </tr>
    </tbody></table>


  <table id="example" class="table text-dark"  style="width:100%;background-color: #FFE8D2">
    <thead class="" style="color: #750404;">
      <tr>
        <th>Customer</th>
        <th>Booking date</th>
        <th>Category</th>
        <th>check In</th>
        <th>Check out</th>
        <th>Bill Amount</th>
        <th>Booking Status</th>
        <th>status</th>
      </tr>
    </thead>
    <tbody>
      {{#each bookingData}}
      <tr class="mt-2">
        <td>{{this.name}}</td>
        <td>{{this.bookings.bookingDate}}</td>
        <td>{{this.bookings.category}}</td>
        <td>{{this.bookings.checkIn}}
        </td>
        <td>{{this.bookings.checkOut}}</td>
        <td>{{this.bookings.billAmt}}</td>
        <td>
          {{this.bookings.bookingStatus}}

        </td>
        {{#if this.bookings.isActive}}
        <td><button class="badge badge-success text-sm" disabled>Active</button></td>
        {{else}}
        {{#if this.bookings.canCancel}}
        <td ><button class="badge badge-danger text-sm " onclick="cancelBooking('{{this.bookings.bookingId}}')">Cancel</button></td>
        {{else}}
        {{#if this.bookings.cancelled}}
        <td><button class="badge badge-primary text-sm" disabled>Cancelled</button></td>
        {{else}}
        {{#if this.bookings.checkedOut}}
        <td>Checked out</td>
        {{/if}}
        {{/if}}
        {{/if}}
        {{/if}}
      </tr> 
      {{/each}}

      </li>

    </tbody>

  </table>
</div>

<script>



</script>